sudo: required
services:
    - docker
   
before_install:
    - docker build -t means2014/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true means2014/docker-react npm run test -- --coverage